# Henry (todofromai-product-manager) — 2026-07-24

Standing-orders session (rule 13); Zidane picked todoFromAI as the least-recently-worked product.
First worklog entry for me in this repo — no prior dailies to read, so I read the fleet CLAUDE.md,
the 22 Jul cost brief, and the CFO's 22 Jul record to get my bearings before touching anything.

## What I did
- `git pull` (fresh), read BACKLOG.md. Confirmed no `develop` branch existed — created it off
  `main`, then cut `feature/cloud-scheduler-cron-tick` off `develop`. Rule 15 respected end to end.
- **Worked backlog #1 (Cloud Scheduler cron refactor) through the full chain.** Wrote the spec
  (`docs/specs/cloud-scheduler-cron-tick.md`) with executable acceptance criteria, then drove the
  build → test → review loop and reviewed the result myself.
  - **Design decision I made:** make the `reminders` table the single firing source of truth and
    fire it from an authenticated `POST /api/internal/cron/tick` that an external Cloud Scheduler
    job hits every ~10 min. Reminders stop riding delayed pg-boss jobs (which can't fire at
    minInstances=0). The same tick also drives digest/poll/cleanup/billing, since scale-to-zero
    breaks every in-process cron, not just reminders.
  - **Two correctness traps I called out in the spec and verified were handled:** (1) no
    double-send when ticks overlap or an awake worker races — solved by an atomic pending→sent
    claim, with revert-to-pending on transient failure (at-least-once); (2) the frequent tick
    would re-send the morning digest many times within a user's target hour — solved by a per-user
    once-per-day guard (`lastSentOn` in notificationPrefs). Both have tests.
- **Verdict: ACCEPT.** Toldo PASS, Samuel APPROVE, I own the merge → merged to `develop`
  (`--no-ff`, 08eae38) and pushed. Deploy/secret/Scheduler-job creation left as the CEO-approved
  release step; noted the handoff to Roberto Carlos for release prep.
- **Investigated the two stale branches Zidane's heartbeat flagged** (no merge/delete — investigate
  only). Both are already fully merged into `main`: `feat/token-project-scope-and-landing`
  (tip 9d2b673, merged via 94ca304) and `worktree-cfo-billing-2026-07-22` (tip e1cbee1, Lehmann's
  CFO record — already on main per rule 12). A third, `worktree-supabase-anon-key`, is likewise
  merged. They are post-merge leftovers; safe to prune, which I recommend routing to space cleanup.

## What I learned / noted for next time
- This repo had **no `develop` branch and no worklog tree** before today — I created both. The
  fleet's Git Flow was only half-instantiated here.
- The test harness needs a real Postgres AND an explicit role in the URL for pg-boss
  (`postgres://root@localhost:5432/...`) — node-postgres won't default the user the way
  postgres-js does. Worth encoding in CI docs so Toldo isn't surprised.
- Docker Hub image pulls are **blocked by egress policy (403)** in the sandbox; I brought up
  Postgres via the OS package instead. Reported, did not route around.

## Unfinished / for the CEO
- CEO decision needed to finish backlog #1: create `CRON_SECRET`, deploy `develop`→`main`, create
  the Cloud Scheduler job (runbook `docs/runbooks/cloud-scheduler.md`). Release prep → Roberto Carlos.
- Recommend HQ/space-manager prune the three already-merged remote branches.
- Next standing item after this ships: backlog #2 (issue triage with Neville).

---

# Session 2 (same day) — CEO instruction intake: UI regen / due default / dashboard / AI status

CEO instruction (Chinese, via the CEO-facing session): regenerate pages in the Claude Code
style; default due = +1 week when unset; the "AI box" tab is useless → project-grouped
dashboard instead; "AI 功能还是没有加上"; confirm carefully with the team. Rule 17: it routes
through me; I investigated the actual state before answering. **No product code written this
session — intake, scoping, backlog, questions only.** Dispatch to Rivaldo comes later.

## What I found (facts, not guesses)
- The tab the CEO calls "AI box" is **"AI Inbox"** (`/inbox-ai` → `TodosView view="ai"`,
  a flat list filtered on `source: 'ai'`). No project-grouped dashboard exists anywhere:
  Agenda groups by TIME (overdue/today/upcoming), sidebar lists projects one by one.
- Due default: `TodoService.create` (packages/core/src/todo-service.ts) leaves `dueAt = null`
  when no `dueAt`/`dueNatural` given; only recurrence derives a date (today-9am baseline).
- **AI is NOT missing** — the CEO's point 4 doesn't match the code. Per-todo AI chat
  (MiniMax-M3) shipped via `feat/ai-chat-billing` (fully merged to `main`): minimax.ts,
  todo-chat-service.ts, chat-routes.ts, TodoChat.tsx inside TodoDetail. apphosting.yaml has an
  ACTIVE `MINIMAX_API_KEY` secret ref; a missing referenced secret stops the backend from
  starting, and prod answers 200 (`/api/health` ok) — so the secret exists and chat should be
  live. Root cause of the CEO's impression, most likely: the assistant lives ONLY inside a
  single todo's detail page, and TodoChat renders nothing on 503 — zero discoverability.
  I did not verify with a logged-in prod session (needs an account); flagged as an action.

## Decisions I took (PM calls, not CEO questions)
- Ranking: approved cron release stays #1 untouched. New CEO items slot 2–5; old 2–4 → 6–8.
- Redesign (#2) and Dashboard (#3) execute as ONE pass — one IA change + one repaint, not two.
- Both start only AFTER the cron release ships develop→main (redesign touches packages/web
  broadly; must not entangle the pending CEO-approved release).
- Due default: +7 days at 09:00 user-local (matches recurrence baseline); explicit
  `dueAt: null` stays null; applies to ALL sources including API/MCP — with the reminder-volume
  consequence flagged in the backlog entry.
- AI Inbox tab is removed per the CEO, but `source: ai` provenance survives as badge/filter —
  "which agent asked" is the product's identity; I won't let the signal die with the tab.

## Rule-16 questions prepared for the CEO (in the brief)
1. Redesign scope: app only / app+landing (recommended) / app+landing+mobile.
2. Dashboard placement: post-login home (recommended) / tab beside Agenda-as-home.
3. What "AI 功能" should mean given per-todo chat already exists: surface existing (recommended)
   / add AI create-and-breakdown / it was a config-visibility miss, verify only.

## Unfinished
- CEO answers to the three questions above; then spec writing + dispatch to Rivaldo.
- Verify AI chat answers in production with a real login (with Toldo next session).
- Cron release execution (unchanged, still the top action).
