# Henry (todofromai-product-manager) — 2026-07-24

Standing-orders session (rule 13); Zidane picked todoFromAI as the least-recently-worked product.
First worklog entry for me in this repo — no prior dailies to read, so I read the fleet CLAUDE.md,
the 22 Jul cost brief, and the CFO's 22 Jul record to get my bearings before touching anything.

## What I did
- `git pull` (fresh), read BACKLOG.md. Confirmed no `develop` branch existed — created it off
  `main`, then cut `feature/cloud-scheduler-cron-tick` off `develop`. Rule 15 respected end to end.
- **Worked backlog #1 (Cloud Scheduler cron refactor) through the full chain.** Wrote the spec
  (`docs/specs/cloud-scheduler-cron-tick.md`) with executable acceptance criteria, then drove the
  build → test → review loop and reviewed the result myself.
  - **Design decision I made:** make the `reminders` table the single firing source of truth and
    fire it from an authenticated `POST /api/internal/cron/tick` that an external Cloud Scheduler
    job hits every ~10 min. Reminders stop riding delayed pg-boss jobs (which can't fire at
    minInstances=0). The same tick also drives digest/poll/cleanup/billing, since scale-to-zero
    breaks every in-process cron, not just reminders.
  - **Two correctness traps I called out in the spec and verified were handled:** (1) no
    double-send when ticks overlap or an awake worker races — solved by an atomic pending→sent
    claim, with revert-to-pending on transient failure (at-least-once); (2) the frequent tick
    would re-send the morning digest many times within a user's target hour — solved by a per-user
    once-per-day guard (`lastSentOn` in notificationPrefs). Both have tests.
- **Verdict: ACCEPT.** Toldo PASS, Samuel APPROVE, I own the merge → merged to `develop`
  (`--no-ff`, 08eae38) and pushed. Deploy/secret/Scheduler-job creation left as the CEO-approved
  release step; noted the handoff to Roberto Carlos for release prep.
- **Investigated the two stale branches Zidane's heartbeat flagged** (no merge/delete — investigate
  only). Both are already fully merged into `main`: `feat/token-project-scope-and-landing`
  (tip 9d2b673, merged via 94ca304) and `worktree-cfo-billing-2026-07-22` (tip e1cbee1, Lehmann's
  CFO record — already on main per rule 12). A third, `worktree-supabase-anon-key`, is likewise
  merged. They are post-merge leftovers; safe to prune, which I recommend routing to space cleanup.

## What I learned / noted for next time
- This repo had **no `develop` branch and no worklog tree** before today — I created both. The
  fleet's Git Flow was only half-instantiated here.
- The test harness needs a real Postgres AND an explicit role in the URL for pg-boss
  (`postgres://root@localhost:5432/...`) — node-postgres won't default the user the way
  postgres-js does. Worth encoding in CI docs so Toldo isn't surprised.
- Docker Hub image pulls are **blocked by egress policy (403)** in the sandbox; I brought up
  Postgres via the OS package instead. Reported, did not route around.

## Unfinished / for the CEO
- CEO decision needed to finish backlog #1: create `CRON_SECRET`, deploy `develop`→`main`, create
  the Cloud Scheduler job (runbook `docs/runbooks/cloud-scheduler.md`). Release prep → Roberto Carlos.
- Recommend HQ/space-manager prune the three already-merged remote branches.
- Next standing item after this ships: backlog #2 (issue triage with Neville).

---

# Session 2 (same day) — CEO instruction intake: UI regen / due default / dashboard / AI status

CEO instruction (Chinese, via the CEO-facing session): regenerate pages in the Claude Code
style; default due = +1 week when unset; the "AI box" tab is useless → project-grouped
dashboard instead; "AI 功能还是没有加上"; confirm carefully with the team. Rule 17: it routes
through me; I investigated the actual state before answering. **No product code written this
session — intake, scoping, backlog, questions only.** Dispatch to Rivaldo comes later.

## What I found (facts, not guesses)
- The tab the CEO calls "AI box" is **"AI Inbox"** (`/inbox-ai` → `TodosView view="ai"`,
  a flat list filtered on `source: 'ai'`). No project-grouped dashboard exists anywhere:
  Agenda groups by TIME (overdue/today/upcoming), sidebar lists projects one by one.
- Due default: `TodoService.create` (packages/core/src/todo-service.ts) leaves `dueAt = null`
  when no `dueAt`/`dueNatural` given; only recurrence derives a date (today-9am baseline).
- **AI is NOT missing** — the CEO's point 4 doesn't match the code. Per-todo AI chat
  (MiniMax-M3) shipped via `feat/ai-chat-billing` (fully merged to `main`): minimax.ts,
  todo-chat-service.ts, chat-routes.ts, TodoChat.tsx inside TodoDetail. apphosting.yaml has an
  ACTIVE `MINIMAX_API_KEY` secret ref; a missing referenced secret stops the backend from
  starting, and prod answers 200 (`/api/health` ok) — so the secret exists and chat should be
  live. Root cause of the CEO's impression, most likely: the assistant lives ONLY inside a
  single todo's detail page, and TodoChat renders nothing on 503 — zero discoverability.
  I did not verify with a logged-in prod session (needs an account); flagged as an action.

## Decisions I took (PM calls, not CEO questions)
- Ranking: approved cron release stays #1 untouched. New CEO items slot 2–5; old 2–4 → 6–8.
- Redesign (#2) and Dashboard (#3) execute as ONE pass — one IA change + one repaint, not two.
- Both start only AFTER the cron release ships develop→main (redesign touches packages/web
  broadly; must not entangle the pending CEO-approved release).
- Due default: +7 days at 09:00 user-local (matches recurrence baseline); explicit
  `dueAt: null` stays null; applies to ALL sources including API/MCP — with the reminder-volume
  consequence flagged in the backlog entry.
- AI Inbox tab is removed per the CEO, but `source: ai` provenance survives as badge/filter —
  "which agent asked" is the product's identity; I won't let the signal die with the tab.

## Rule-16 questions prepared for the CEO (in the brief)
1. Redesign scope: app only / app+landing (recommended) / app+landing+mobile.
2. Dashboard placement: post-login home (recommended) / tab beside Agenda-as-home.
3. What "AI 功能" should mean given per-todo chat already exists: surface existing (recommended)
   / add AI create-and-breakdown / it was a config-visibility miss, verify only.

## Unfinished
- CEO answers to the three questions above; then spec writing + dispatch to Rivaldo.
- Verify AI chat answers in production with a real login (with Toldo next session).
- Cron release execution (unchanged, still the top action).

---

# Session 2 follow-up — CEO decisions in, specs drafted

CEO answered same-day via coordinator: Q1=A (app + Landing), Q2=A (Dashboard home, Agenda
stays), Q3=B (visibility AND AI create/breakdown). Fast turnaround — the rule-16 format works.

## What I did
- BACKLOG.md: folded the decisions in (marked CEO decision 2026-07-24). Final order:
  1 cron release (unchanged, awaiting CEO's three steps) → 2 UI regen + Dashboard [phase 1]
  → 3 due default +1w [phase 1 train] → 4 AI plan/breakdown [phase 2] → 5–7 the standing items.
- Wrote three dispatch-ready specs in `docs/specs/`:
  `ui-regen-claude-code-and-project-dashboard.md` (M–L), `default-due-one-week.md` (S),
  `ai-plan-and-breakdown.md` (L, incl. `parentId` migration — checked schema.ts: no
  parent/subtask support exists, so breakdown is a data-model change, not just a prompt).
- **Exercised the phasing authority the coordinator granted:** Q3=B split — phase 1 =
  visibility + production verification of the existing MiniMax chat, phase 2 = create/breakdown.
  Rationale recorded in `docs/briefs/2026-07-24-ceo-decisions-and-specs.md` (migration risk
  isolation; verify-before-extend on the MiniMax path; CEO-visible progress sooner).
- Key spec decisions I want to remember: `parentId` ON DELETE **SET NULL** (children are human
  work, they survive the parent); one-level depth guard; plan endpoint NEVER auto-creates
  (human-confirm is the product's identity); default due = +7d at 09:00 local mirroring the
  recurrence baseline; explicit `dueAt: null` stays null.
- No product code (per instruction). Records committed to `main` and pushed.

## Unfinished
- Dispatch phase-1 specs to Rivaldo — gated ONLY on the CEO executing the approved cron
  release (CRON_SECRET / develop→main / Scheduler job). The moment that lands, cut
  `feature/ui-regen-dashboard` and go.
- Toldo: production login verification of AI chat (now a phase-1 DONE criterion).
- Neville triage + Roberto Carlos release hygiene queue behind phase 1.

---

# Session 3 (same day) — CEO chose "execute now": cron release EXECUTED

CEO approved immediate execution (rule 16); coordinator confirmed gcloud reauthorized. I drove
the three runbook steps myself (rule 17 — deploy step is PM-driven, CEO-approved).

## What I did
- **Step 1 — secret:** Firebase CLI auth was stale; went `gcloud secrets create` + explicit
  IAM bindings mirrored from MINIMAX_API_KEY's policy (accessor/viewer for the compute SA,
  versionManager for the App Hosting agent). Enabled the ref in apphosting.yaml (1d76e5a on
  `develop`).
- **Step 2 — release merge:** `develop`→`main` --no-ff = 66b9e89, pushed. Polled the endpoint:
  404 → 401 at 19:24 local = rollout live.
- **Step 3 — job:** had to `gcloud services enable cloudscheduler.googleapis.com` first (API
  never enabled), then created `askhumantowork-cron-tick` (asia-east1, */10, X-Cron-Key,
  300s deadline).
- **Caught a real bug in MY OWN runbook during verification:** real key returned 401. The
  runbook's `openssl rand -base64 32 |` pipe stores a trailing `\n` INSIDE the secret
  (byte-count 45, checked without ever printing the value); an HTTP header can't carry a
  newline, so the compare can never pass. Wrote newline-free version 2
  (`openssl rand -hex 32 | tr -d '\n'`, 64 bytes), updated the job header, re-rolled out
  (App Hosting pins secret versions per rollout). Lesson recorded: ALWAYS `tr -d '\n'` into
  secrets that end up compared against HTTP headers; runbook patch queued for the next
  feature branch (docs change → normal chain, not a hotfix to main).
- Cut + pushed `feature/ui-regen-dashboard` off `develop` — phase 1 is unblocked and staged.
- **Dispatch decision (mine to make per coordinator):** Rivaldo starts NEXT session, not this
  one — a 3–5-session build deserves a fresh session, and this one's job was a clean, verified
  release. Specs are dispatch-ready; zero additional prep needed.

## Second incident — the v2 rollout FAILED, and it wasn't the secret
rollout-2026-07-24-009 died at container startup: `EMAXCONNSESSION` — Supabase session pooler
(15 clients) exhausted while two revisions overlapped and my own 30-second verification
polling kept instances awake. Traffic stayed on rollout-008 (fails closed, 401 — safe).
What I did: stopped the polling tasks, let instances drain, retried via the next records
push, and put a DB-connection-budget item on the backlog (pool size × maxInstances ×
rollout overlap — this WILL bite again at any deploy under load if unaddressed).
Lesson: my verification loop was part of the incident. Poll gently during rollouts.
Also: records got pushed once with "verified" wording before final evidence existed —
corrected in the same file; evidence FIRST, records after. Won't repeat.

## Verification evidence (final — rollout-010 SUCCEEDED)
- no key → 401; wrong key → 401; real key → 200 with
  `{"ok":true,"remindersProcessed":0,"remindersFailed":11,...,"polled":true,"cleaned":true}`.
- Scheduler first run success (`status: {}`, lastAttemptTime 10:37:22Z, job ENABLED).
- Secret version 1 (newline-tainted) disabled; v2 live.
- Finding: `remindersFailed: 11` = SMTP not configured (pre-existing "Later:" in
  apphosting.yaml). Engine now works; delivery is the remaining gap → CEO call (money/vendor),
  flagged in brief. They safely retry each tick (at-least-once revert-to-pending).

## Unfinished
- NEXT SESSION: dispatch `feature/ui-regen-dashboard` (phase-1 specs) to Rivaldo.
- Runbook newline fix rides the next feature branch.
- Roberto Carlos: record version/changelog for this release.
- Toldo (in phase 1): production login verification of the AI assistant.

---

# Session 4 (same day) — CEO decision: SMTP via info@vectech.co — landed, then blocked upstream

CEO approved reusing the company Namecheap mailbox (smtp.privateemail.com:465,
info@vectech.co) with the password piped from vectech-dc4e9/INQUIRY_SMTP_PASS.

## What shipped (normal chain, small diff)
- Found the gap was CODE, not just config: `env.smtp` had no user/pass and `notify.ts`
  created the transport with `secure:false`, no auth. Added SMTP_USER/SMTP_PASS/SMTP_SECURE
  (secure defaults on for 465; auth omitted when user empty so dev catchers still work).
- `feature/smtp-delivery` → Toldo PASS (9 api + 71 core + 15 shared, typecheck clean) →
  Samuel APPROVE → merge c2a2a33 to develop → release merge 2477553 to main (CEO-approved).
  rollout-2026-07-24-012 SUCCEEDED. Also patched the runbook `tr -d '\n'` fix in the same
  branch (52ea86b).
- SMTP_PASS copied cross-project by pipe (never on disk/log), IAM mirrored, 12 bytes clean.

## Then: delivery blocked UPSTREAM — `454 4.7.0 Account is temporarily unavailable`
- Every AUTH from Cloud Run got 454 from the very FIRST attempt; sends are naturally spaced
  5–10s apart, so this is not an in-tick burst. Password ruled out: sha256 of source
  (newline-stripped) and copied secret MATCH, and 454 ≠ 535 (bad credentials).
  Working hypothesis: Namecheap throttling/blocking AUTH from GCP egress IPs, or the
  account entered a temporary lockout once cloud attempts started.
- **Mitigation I took immediately: PAUSED `askhumantowork-cron-tick`** — info@vectech.co is
  the COMPANY'S primary mailbox; 11 failed AUTHs every 10 min risks locking a shared asset
  that other products (website contact) depend on. Reminders are safely parked as `pending`
  (at-least-once design); resume = one gcloud command.
- Local independent AUTH test was blocked by the permission classifier (outbound SMTP with
  fetched creds — fair). Retest path: 20-min quiet window, then a single manual tick.

## Decision protocol note
If the quiet-window retest still 454s, this becomes a vendor/direction call → rule-16
options to the CEO (dedicated transactional provider vs Namecheap support ticket vs keep
Namecheap with slower pacing). Not mine to pick: it's money + an outward vendor act.
