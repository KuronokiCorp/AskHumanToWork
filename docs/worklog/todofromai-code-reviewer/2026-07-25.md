# 2026-07-25 — Samuel (todofromai-code-reviewer)

## Reviewed: `feature/ui-regen-dashboard` → develop (3 commits)
Spec: `docs/specs/ui-regen-claude-code-and-project-dashboard.md` (+ CEO mid-build scope
update: Landing EXCLUDED from the dark restyle; AC5 downgraded to non-regression).

**VERDICT: ACCEPT.** Tester already green (web e2e 37 / api 12 / core 71 / shared 15,
full typecheck). I re-ran `web typecheck` — clean.

### What I verified
- **All 7 ACs conform.** Dashboard is post-login home; `/`,`/login`,`/auth/callback`,
  `/landing`,`/inbox-ai` → `/dashboard`; sidebar drops AI Inbox; `view="ai"` path fully
  retired (only the `source` filter uses `'ai'` now). Grouping/sort in
  `DashboardView.buildGroups` matches spec (overdue-first, open-count desc, No-project
  sunk last, dueAt ASC NULLS-LAST via Infinity then priority desc, 8/group + more).
  AI badge + source filter, `#assistant` id on TodoChat with "AI assistant" header.
  No schema/API-shape change (`createdByToken` already in shared schema).
- **Landing isolation holds.** Landing.tsx + components/landing/* untouched in the diff;
  authed dark shell is scoped to the App wrapper (`bg-zinc-950`), body stays light,
  Login/ResetPassword got local light `inputCls`. Shared `inputCls` darkened only affects
  authed pages.
- **No undefined accent shades** (200–700 used, all defined); no dangling `shadow-glow`;
  oauth/chat test edits are legit consequences of the IA + header rename, not bent to pass.

### Nits (non-blocking, for a later pass)
1. `#assistant` has no SPA scroll-to-hash handler — row sparkle navigates to the detail
   page but won't jump to the panel on client-side nav (header is visible, so AC4 still met).
2. Row sparkle is `opacity-0 group-hover:opacity-100` with no focus-visible/touch reveal —
   keyboard/touch discoverability gap.
3. `index.css ::selection` still `bg-violet-200` (cosmetic leftover).
4. Digest §2.4 guard is trivially green (template carries no app links) — honest regression
   guard, fine to keep.

Verdict returned to Henry for the develop merge.
