# 2026-07-25 — Toldo (todofromai-tester)

## Verified: phase-1 UI regen on `feature/ui-regen-dashboard`

### New tests written
- `tests/e2e/dashboard.spec.ts` — the four dashboard/IA acceptance criteria:
  - **AC1** `/` lands on `/dashboard`; sidebar shows Dashboard/Agenda/All todos and NO
    "AI Inbox"; `/inbox-ai` redirects to `/dashboard`.
  - **AC2** 2 projects + ungrouped todos → one group per project + "No project"; counts
    match seed; overdue count renders and that group sorts first.
  - **AC3** a `source:'ai'`, `createdByAgent:'heyhuman'` todo shows the agent badge on
    dashboard + All todos; the source filter (all/human/ai) narrows correctly. (Seeded via
    a session request carrying `x-todo-source`/`x-agent-name` headers.)
  - **AC4** group header → project view; row → `/t/:id` with a visible "AI assistant" header.
- `packages/api/src/digest.test.ts` — rendered digest template carries no `/inbox-ai`
  (nor `/agenda`) links (**AC6**).
- Updated `oauth.spec.ts` (post-OAuth home `/agenda`→`/dashboard`) and `chat.spec.ts`
  (panel header "Ask about this task"→"AI assistant") — legitimate consequences of the
  IA/header change, not tests bent to pass.

### Results (all green)
- web e2e **37 passed** (incl. the 12 Landing tests → **AC5** non-regression: Landing
  still renders and nav works, unrestyled).
- api **12**, core **71**, shared **15**; full monorepo typecheck clean (**AC7**).
- One flake seen once under full-parallel load (`oauth: signing in twice` — OAuth-stub
  timing); passes in isolation and re-run, and CI runs `retries:1`. Not a code defect.

### Residual (honest)
- **Spec §4 production verification** — that the per-todo AI chat *answers* (not 503, not
  silent) on the live site with a real login — is **NOT done**: no prod login from this
  environment, and prod still serves the OLD UI (nothing to verify against yet). Must run
  after the CEO-approved `develop→main` deploy. Chat is proven locally (chat.spec, 5 green).
