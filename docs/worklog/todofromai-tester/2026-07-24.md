# Toldo (todofromai-tester) — 2026-07-24

Verified backlog #1 (`feature/cloud-scheduler-cron-tick`) against the spec's acceptance criteria.
Driven by Henry this session (no Task tool in the sandbox to run me as a separate process); logging
the verification here so the test gate is attributed.

## Environment
- Real Postgres 16 (installed via OS package — Docker Hub image pulls are blocked by egress policy
  in this sandbox, reported not worked around). Cluster on :5432, role `root`, db
  `askhumantowork_test`. pg-boss needs the role in the URL:
  `postgres://root@localhost:5432/askhumantowork_test`.
- SMTP: a tiny in-process sink on :1025 inside the test counts real message deliveries (Mailpit
  image also blocked). Reminders tested on the `web_push` channel, which is a no-op success when
  VAPID is unconfigured — isolates the firing/claim logic from a live push service.

## Results — ALL GREEN
- **Full suite:** shared 15 ✓, core 71 ✓ (regression — the reminder ladder/cancellation tests
  still pass after `enqueue` became a no-op), api 9 ✓.
- **New `packages/api/src/cron.test.ts` (9):**
  - **Delayed-reminder (headline):** a `pending` reminder with `fireAt` in the past (came due while
    the instance was asleep) is delivered on the next tick and flips to `sent`. ✓
  - Future `fireAt` is **not** delivered (stays pending). ✓
  - **No double-send:** two ticks fired concurrently (`Promise.all`) on one due reminder deliver it
    exactly once (combined processed === 1). ✓
  - **Quiet hours:** reminder is deferred (stays pending), not sent. ✓
  - **Done todo:** reminder is cancelled, not sent. ✓
  - **Digest once-per-day:** three ticks inside the user's target local hour send exactly one
    digest email (sink counted 1); `lastSentOn` stamped to today. ✓
  - **Endpoint auth:** 503 when `CRON_SECRET` unset, 401 on missing/wrong key, 200 + summary with
    a correct Bearer or `X-Cron-Key`. ✓

## Verdict: **PASS.**
No defects. Note for CI: encode the `postgres://<role>@...` URL requirement so pg-boss connects.
