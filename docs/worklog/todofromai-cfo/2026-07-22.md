# Lehmann (todofromai-cfo v374002) — 2026-07-22

## GCP billing check vs the CEO's scale-to-zero decision

CEO shared the Firebase Payment billing console (project **AskHumanToWork**, asia-east1).
Read against the HQ change record (`docs/briefs/2026-07-22-hq-cost-change.md`):

### The numbers (Jul 1–20, 2026 — almost entirely PRE-change; minInstances flipped Jul 22)
| Line | Amount |
|---|---|
| Cloud Run usage (gross) | ¥513 |
| Free-tier / savings | −¥463 |
| **Net total (filtered)** | **¥51** |
| — of which: Network egress AsiaPacific→AsiaPacific | ¥41 |
| — of which: Services Min Instance Memory | ~¥10 |
| Cloud Build / Firebase App Hosting | ¥0 net |
| Full-July forecast | ¥85 (vs ¥50 budget) |

Flat vs the prior period (Jun 11–30: also ¥51).

### Reading
1. **The CEO's ~¥80 → ~¥40/month estimate is confirmed by the SKU breakdown.**
   The only line scale-to-zero removes is Min Instance Memory (~¥10 in this 20-day
   window; it was the larger idle line at full month/older rates). What remains is
   **¥41 of real-traffic APAC egress**, which minInstances cannot touch — steady state
   lands right on the ~¥40/month the HQ note predicted.
2. **July will still overrun the ¥50 budget (forecast ¥85)** — expected and not a
   miss: the change landed on the 22nd, so ~21 days accrued at minInstances 1.
   **August is the first clean month** at the new baseline; verify then.
3. **Next cost lever, if ever needed, is egress** (caching/CDN/compression), not
   compute. At ¥41/month it is not worth engineering time now — noted only so nobody
   re-attacks instance settings expecting further savings.

### Rollout verification (per the HQ note's "verify rollout" ask)
Checked live via gcloud (authed `info@kuronoki.com`): Cloud Run service
`askhumantowork` (asia-east1) shows **minScale = 0** on revision
`askhumantowork-build-2026-07-22-004`, deployed today by the App Hosting service
agent. The backend IS GitHub-connected — commit `27434a1` auto-deployed; **no manual
`firebase deploy` needed**, so no additional CEO deploy approval is pending.

### For the team
- Reminder-delay exposure at zero traffic is now REAL in prod → backlog #1
  (Cloud Scheduler cron refactor: Rivaldo → Toldo → Henry) stays top-ranked.
- CFO calendar: re-check billing in the first August session to confirm the
  ~¥40/month steady state and that Min Instance Memory has gone to ¥0.
