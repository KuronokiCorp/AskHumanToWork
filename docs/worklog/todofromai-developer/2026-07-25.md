# 2026-07-25 — Rivaldo (todofromai-developer)

## Built: phase-1 UI regen (spec `ui-regen-claude-code-and-project-dashboard.md`)
Branch `feature/ui-regen-dashboard`, cut fresh off the synced `develop` (main's cron +
SMTP releases merged into develop first, per spec §6 sequencing guardrail). The branch was
found empty last session (spec written, zero implementation) — so this was the real build.

### What I implemented
- **Token layer** (`tailwind.config.js`): added a terracotta `accent` scale (100–700,
  DEFAULT `#CC785C`) — the single warm hue. Everything else stays zinc.
- **Dark surface, isolated** (`ui.tsx`, `App.tsx`): repainted the shared primitives
  (Button/Chip/SectionCard/EmptyState/inputCls/Logo) to dark + accent, and dark-scoped the
  authed app shell (`bg-zinc-950`). Kept `body` light so the **Landing intro page stays
  untouched** (CEO scope update 25 Jul) and gave Login/ResetPassword local light inputs.
- **IA** (`App.tsx`): `/dashboard` is the post-login home; `/`,`/login`,`/auth/callback`,
  `/landing`,`/inbox-ai` all redirect to it; AI Inbox nav removed; `TodosView view="ai"`
  retired. Login post-OAuth redirect → `/dashboard`.
- **New `DashboardView`**: open todos grouped per project (named vs ungrouped kept in
  separate buckets so a project literally named "none" can't collide), overdue-first
  ordering, mono `n open` / `n overdue`, 8/group + "+ n more", header links to the project.
- **AI provenance & assistant** (`TodoItem`, `TodoChat`, `TodoDetail`): `⚡{agent}` mono
  badge, source filter (all/human/ai) on All todos, row sparkle → `/t/:id#assistant`, a
  visible "AI assistant" panel header, and a scroll-to-hash so the sparkle lands on it.
- **QuickAdd** restyled as a `>` prompt line. Agenda calendar + Settings pages repainted to
  dark tokens (Settings via a conservative scripted token pass).
- No schema/API-shape change (spec §6 honored).

### Snags
- The Write/Edit path in this environment twice injected a stray NUL byte into
  `DashboardView.tsx` (git flagged it binary) — stripped both times and re-verified the
  committed blob is clean UTF-8.
- Mid-review the machine hit **disk-full (ENOSPC)**; reclaimed space (build artifacts) and
  resumed. Noted for ops.

Handed to Toldo (test) → Samuel (review). Both green. Henry merged to `develop`.
